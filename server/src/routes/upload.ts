import { Router } from 'express';

const router = Router();

// POST   /api/upload/image      - upload product image (admin)
// DELETE /api/upload/image/:key - delete uploaded image (admin)

export default router;
